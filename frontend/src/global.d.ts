// Déclarations globales pour TypeScript

declare global {
  interface Window {
    google: any;
  }
}

// Cette export vide est nécessaire pour que TypeScript traite ce fichier comme un module
export {};
